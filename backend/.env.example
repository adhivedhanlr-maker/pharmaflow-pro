# Database (Production: PostgreSQL, Development: SQLite)
# For Postgres: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="file:./dev.db"

# JWT Security
JWT_SECRET="CHANGE_ME_TO_A_LONG_RANDOM_STRING"
JWT_EXPIRES_IN="1d"

# SMTP Configuration (Production Alerts)
SMTP_HOST="smtp.example.com"
SMTP_PORT=587
SMTP_USER="notifications@pharmaflow.pro"
SMTP_PASS="your_secure_password"
SMTP_FROM="PharmaFlow Pro <alerts@pharmaflow.pro>"

# Application Settings
FRONTEND_URL="http://localhost:3000"
NODE_ENV="development"
